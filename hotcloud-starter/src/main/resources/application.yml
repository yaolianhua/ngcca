spring:
  application:
    name: hotcloud
  profiles:
    include:
      - kubernetes
      - buildpack
      - message
      - security
  rabbitmq:
    host: ${message.rabbitmq.host:}
    port: ${message.rabbitmq.port:}
    username: ${message.rabbitmq.username:}
    password: ${message.rabbitmq.password:}
    virtual-host: /
## log level
logging:
  level:
    io.hotcloud.kubernetes.server: debug
    io.hotcloud.message: info